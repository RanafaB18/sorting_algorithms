#include "sort.h"

void insertion_sort_list(listint_t **list)
{
    listint_t *head = *list;
    listint_t *prev = *list;
    listint_t *next = head->next;

    while (head)
    {
        if (head->n > next->n)
        {
            
        }
    }
}
